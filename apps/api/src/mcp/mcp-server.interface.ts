/**
 * Re-exports MCP types from @finch/types for convenience within the MCP module.
 * All canonical type definitions live in @finch/types (W5A-01).
 */
export type { MCPServer, MCPTool, MCPToolPermission, MCPToolCallAuditPayload } from '@finch/types';
